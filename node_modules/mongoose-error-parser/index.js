module.exports = function parse(error) {
	if (!error) return null;

	const _errors = error.errors || error;

	const fields = Object.keys(_errors);
	const errors = fields.map(f => _errors[fields]);

	return {
		fields: fields,
		errors: errors
	}
}